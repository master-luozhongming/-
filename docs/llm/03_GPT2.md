# GPT-2

> 原书 PDF 第 37–62 页
> 人话版：[Modelshit Class · GPT-2：把 Transformer 拆开看](https://modelshit-class.vercel.app/01-llm/03-gpt2/)

## 1. 一句话

Decoder-only Transformer：因果注意力、LayerNorm、FFN、位置编码与训练循环。

## 2. 章节结构

- 3.1 GPT-2 模型结构的定义以及数据集的准备
- 768 个元素的向量
- 3.2 GPT-2 模型的训练
- 3.3 加载OpenAI 开源的GPT-2 预训练权重

## 3. 核心要点

1. 3. GPT-2 3.1 GPT-2 模型结构的定义以及数据集的准备 创建一个文件gpt_model.py 。 我们首先导入需要的依赖。 1 import tiktoken # 分词器库 Python 2 import torch 3 import torch.nn as nn 4 from torch.utils.data import Dataset, DataLoader 3.1.1 数据集的准备 图 3.1 分词

2. 38 Chapter 3. GPT-2 接下来我们构建数据集。 1 class GPTDatasetV1(Dataset): Python 2 def __init__(self, txt, tokenizer, max_length, stride): 3 self.input_ids = [] 4 self.target_ids = [] 5 6 # 对整个文本进行分词 7 token_ids = tokenizer.encode(txt, allowed_special={"<|endoftext|>"}) 8 9 # 使用滑动窗口将文本切割成**重叠的**长度为`max_length`的序列 10 for i in range(0, len(token_ids) - max_length, stride): 11 input_chunk = token_ids[i : i + max_length] 12 target_chunk = token_ids[i + 1 : i + max_length + 1] 13 self.input_ids.append(torch....

3. 3.1 GPT-2 模型结构的定义以及数据集的准备 39 图 3.2 数据集 3.1.2 模型结构 接下来我们自顶向下的来定义模型结构。 1 class GPTModel(nn.Module): Python 2 def __init__(self, cfg): 3 super().__init__() 4 self.tok_emb = nn.Embedding(cfg["vocab_size"], cfg["emb_dim"]) 5 self.pos_emb = nn.Embedding(cfg["context_length"], cfg["emb_dim"]) 6 self.drop_emb = nn.Dropout(cfg["drop_rate"]) 7 8 self.trf_blocks = nn.Sequential( 9 *[TransformerBlock(cfg) for _ in range(cfg["n_layers"])]) 10 11 self.final_norm = LayerNorm(cfg["emb_dim"]) 12 self.out_head...

4. 40 Chapter 3. GPT-2 3.1.2.1 词嵌入 首先GPT-2 模型的输入in_idx 是一批token id 列表，形状为(batch_size, seq_len) 。 所以batch_size, seq_len = in_idx.shape 这一行可以提取批次的大小batch_size 和每条数据 序列的长度seq_len 。 而 tok_embeds = self.tok_emb(in_idx) 将输入的token id 列表转换成了形状为 (batch_size, seq_len, emb_dim) 的张量。 nn.Embedding 的作用是将token id 转换成一个向量，如GPT-2 会将一个token id 转换成 一个768 个浮点数元素的向量。 图 3.3 词嵌入 图 3.4 一条数据和一批数据的词嵌入示例 在PyTorch 中，嵌入层实现的功能与执行矩阵乘法的线性层相同；我们使用嵌入层主要是出于计 算效率的考虑。 1 # 假设我们有以下3个训练样本， Python 2 # 它们可能代表LLM上下文中的token ID 3 idx = tor...

5. 3.1 GPT-2 模型结构的定义以及数据集的准备 41 7 # 如果最大的token ID是3，那么我们需要4行，用于可能的 8 # token ID：0, 1, 2, 3 9 num_idx = max(idx)+1 10 11 # 期望的嵌入维度是一个超参数 12 out_dim = 5 实现一个简单的嵌入层 1 torch.manual_seed(123) Python 2 embedding = torch.nn.Embedding(num_idx, out_dim) 3 print(embedding.weight) # 查看嵌入权重 4 # 获取训练示例ID为1的向量表示 5 print(embedding(torch.tensor([1]))) 6 # 获取训练示例ID为2的向量表示 7 print(embedding(torch.tensor([2]))) 8 # 查找一批ID的向量表示 9 idx = torch.tensor([2, 3, 1]) 10 print(embedding(idx)) 现在我们用独热编码和nn.Linear 实现和上面的嵌入层一样...

6. 42 Chapter 3. GPT-2 token id 的索引 位置嵌入向量 0 768 个元素的向量 1 768 个元素的向量 2 768 个元素的向量 … … 255 768 个元素的向量 表 3.1 上下文长度为256 的位置嵌入查找表 所以 1 pos_embeds = self.pos_emb(torch.arange(seq_len, device=in_idx.device)) Python 为[0, 1, ..., seq_len - 1] 中的每个位置分配了一个768 元素的向量，作为对应的位置编码。也 就是将序列的每个位置编号组成的序列变换成形状(batch_size, seq_len, emb_dim) 。 然后我们就会将token 的词嵌入向量和token 所在位置的位置嵌入向量直接相加。 1 x = tok_embeds + pos_embeds Python 初看起来，在token 向量上面添加位置信息会破坏输入向量，使得网络的任务变得更加难以完成。 不过，从两个随机选择的不相关向量在高维空间中几乎是正交的这一现象中，我们可以了解到这一 方法为什么能...

7. 3.1 GPT-2 模型结构的定义以及数据集的准备 43 送进 transformer 模块的是 token 嵌入+位置嵌入，形状为： (batch_size, seq_len, emb_dim) 。 图 3.5 进入self.trf_blocks 的数据 接下来我们编写TransformerBlock 的代码 1 class TransformerBlock(nn.Module): Python 2 def __init__(self, cfg): 3 super().__init__() 4 self.att = MultiHeadAttention( 5 d_in=cfg["emb_dim"], 6 d_out=cfg["emb_dim"], 7 context_length=cfg["context_length"], 8 num_heads=cfg["n_heads"], 9 dropout=cfg["drop_rate"], 10 qkv_bias=cfg["qkv_bias"], 11 ) 12 self.ff = FeedForward(cfg) 13 self....

8. 44 Chapter 3. GPT-2 28 x = self.ff(x) 29 x = self.drop_shortcut(x) 30 x = x + shortcut # 将原始的输入加过来 31 32 return x

## 4. 与前后章关系

- 在线阅读：https://modelshit-class.vercel.app/01-llm/03-gpt2/
- 本地对照：`python tools/extract_pdf.py "E:\大模型\强化学习\main2.pdf" --start 37 --end 62`
