# 大模型训练全流程

> 原书 PDF 第 149–154 页
> 人话版：[Modelshit Class · 大模型训练全流程](https://modelshit-class.vercel.app/02-align/10-llm-training/)

## 1. 一句话

预训练 → SFT → RLHF/DPO/GRPO，对齐技术选型一览。

## 2. 章节结构

- 10.1.1 预训练
- 10.1.2 监督微调
- 10.1 LLM 训练流程
- 10.2.1 近端策略优化（PPO）
- 10.2.2 直接偏好优化（DPO）
- 10.2.3 组相对策略优化（GRPO）
- 10.2 RLHF 的关键技术

## 3. 核心要点

1. 10. 大语言模型训练概述 10.1 LLM 训练流程 预训练大语 言模型 SFT RLHF 示例数据集 人类反馈数 据集 用户 数据飞 起 图 10.1 大语言模型训练流程 10.1.1 预训练 假设训练数据是“abcd”，那么预测下一个token 的原理如下 a b c ↓ ↓ ↓ b c d 也就是我们希望如下的目标函数越大越好 𝐽(𝜃) = 𝜋𝜃(𝑏|𝑎)𝜋𝜃(𝑐|𝑎, 𝑏)𝜋𝜃(𝑑|𝑎, 𝑏, 𝑐) (10.1) 从这里我们可以看出，在LLM 这个环境中

2. 150 Chapter 10. 大语言模型训练概述 • 策略𝜋𝜃是LLM • 状态是上下文，例如𝜋𝜃(𝑑|𝑎, 𝑏, 𝑐)的状态𝑆𝑡= 字符串abc • 动作：𝜋𝜃(𝑑|𝑎, 𝑏, 𝑐)表示在状态𝑆𝑡= 字符串abc的情况下，𝐴𝑡为输出字符“d”。 • 动作概率：𝜋𝜃(𝑑|𝑎, 𝑏, 𝑐)为采取动作的概率。 10.1.2 监督微调 假设训练数据是“How are you? I am fine.<eos_token>”。 其中指令部分为“How are you?”。 回答部分为“I an fine.<eos_token>”。 那么训练方式如下： How are you ? I am fine . ↓ ↓ ↓ ↓ ↓ ↓ ↓ ↓ are you ? I am fine . <eos_token> 只有绿色部分计算损失。也就是我们要让下面的式子越大越好 𝐽(𝜃) = 𝜋𝜃(I|How are you?) × 𝜋𝜃(am|How are you? I) × 𝜋𝜃(fine|How are you? I am) × 𝜋𝜃(.|How are you? I am fine) × 𝜋𝜃(<eo...

3. 10.1 LLM 训练流程 151 RLHF RLHF: Reinforcement Learning From Human Feedback 当我们谈论大语言模型（LLM）的强化学习时，我们进入了一个完全不同的世界。我们不再训练智 能体在倒立摆环境中的表现，而是对预训练好的大语言模型进行微调，使其符合人类的偏好。该模 型不会与外部环境交互——它本质上是在探索自身的输出空间。 正如OpenAI 和其他科研人员所发现的，这种方法对于将原始语言模型转化为辅助系统至关重 要。正如IBM 研究人员指出的那样，“RLHF 特别适合于目标复杂、定义不明确或难以指定的任务。” 毕竟，如何用数学来定义“乐于助人”或“诚实”这样的概念呢？ 这里的根本转变是： 1. 我们正在优化大语言模型以适应人类的偏好，而不是对环境的掌控 2. 我们的数据来自人类的判断，而不是环境互动 3. 我们需要在奖励最大化与保持接近原始预训练行为之间取得平衡 这种平衡行为使得LLM 强化学习特别棘手，但也特别令人着迷！ 10.2 RLHF 的关键技术 预训练大语言模型 近端策略优化(PPO) 直接偏好优化(DPO) 组相对...

4. 152 Chapter 10. 大语言模型训练概述 4. 使用PPO 微调LLM，来最大化奖励，同时保持接近原来的LLM。 10.2.2 直接偏好优化（DPO） 如果说PPO 是一位谨慎的外科医生，能够精准地进行手术，那么DPO 则是一位效率专家，他找到 了通往同一目标的捷径。DPO 于2023 年在一篇题为《直接偏好优化：你的语言模型其实是一个奖 励模型》的论文中首次提出，它彻底消除了对单独奖励模型的需求。 DPO的精妙之处在于其数学洞察力：奖励函数与最优策略之间存在直接映射。通过利用这种关系， DPO 将强化学习问题转化为基于人类偏好数据的更简单的分类问题。 与传统的三步RLHF 流程不同，DPO 只需一个训练阶段即可实现相同的目标。这就像跳过中间环 节，直接到达源头。 DPO 对从业者特别有吸引力的原因是： 1. 简单：无需训练单独的奖励模型 2. 效率：无需在训练期间进行昂贵的采样 3. 稳定性：组件越少，出错的可能性就越小 4. 性能：在控制输出属性方面，通常与PPO-RLHF 相当或超过PPO-RLHF DPO 的目标函数如下： 𝐽DPO(𝜋𝜃; 𝜋ref) = 𝔼(...

5. 10.2 RLHF 的关键技术 153 ℒ︀GRPO(𝜃) = 1 𝐺∑ 𝐺 𝑖=1 1 |𝑜𝑖| ∑ |𝑜𝑖| 𝑡=1 min[𝑝𝐴̂𝑖,𝑡, clip(𝑝, 1 −𝜀, 1 + 𝜀)𝐴̂𝑖,𝑡] −𝛽𝐷KL[𝜋𝜃∥𝜋ref] 其中比值 𝑝= 𝜋𝜃(𝑜𝑖,𝑡|𝑞, 𝑜𝑖,<𝑡) 𝜋𝜃old(𝑜𝑖,𝑡|𝑞, 𝑜𝑖,<𝑡) 裁剪替代损失的token 平均损失 • 遍历𝐺个采样的回答，并将每个回答的token 平均损失相加 • 除以𝐺，来获得所有采样的回答的平均值，这样保证了每个回答𝑜𝑖的贡献的单位都一样 某个回答的每个token 都计算损失，并取平均值 • 将回答𝑜𝑖的所有token 都计算一遍损失，然后相加 • 除以token 的数量：|𝑜𝑖| = len(𝑜𝑖)，这样保证了每个token 都有相同单位的贡献 组内优势 (10.6) 图 10.4 GRPO 替代损失函数 现在，如果我们能将 PPO 的可靠性与更高的效率以及对推理能力提升的专注结合起来，会怎么样 呢？GRPO 应运而生，它是强化学习领域的最新成果之一，由 DeepSeek 开发，并用于训练其令 人印象深刻的 Dee...

## 4. 与前后章关系

- 在线阅读：https://modelshit-class.vercel.app/02-align/10-llm-training/
- 本地对照：`python tools/extract_pdf.py "E:\大模型\强化学习\main2.pdf" --start 149 --end 154`
