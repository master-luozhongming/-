# PPO 背后的数学

> 原书 PDF 第 125–140 页
> 人话版：[Modelshit Class · PPO 背后的数学](https://modelshit-class.vercel.app/01-rl/08-ppo-math/)

## 1. 一句话

KL 散度、重要性采样、替代目标函数与误差上界。

## 2. 章节结构

- 0.5 + 0.3 log 0.3
- 8.1 数学背景知识
- 0.2 + 0.3 log 0.3
- 0.7 + 0.3 log 0.3
- 8.2 替代目标
- 2 ∑
- 8.3 补充知识

## 3. 核心要点

1. 8. PPO 背后的数学 在使用策略梯度算法训练智能体时，一个挑战是它们容易出现性能崩溃：智能体会突然表现很 差。这种情况很难恢复，因为智能体会开始生成质量较差的轨迹，而这些轨迹又被用于进一步训练 策略。我们还看到，on-policy（同策略）算法的样本效率较低，因为它们无法重用数据。 Schulman 等人提出的近端策略优化（Proximal Policy Optimization, PPO）是一类优 化算法，用来解决这两个问题。PPO 的核心思想是在目标函数中引入一个替代（surrogate）目标， 它通过保证策略的单调改进来避免性能崩溃。该目标还有一个好处，就是可以在训练过程中重用离 策略（off-policy）数据。 PPO 可以通过将原始的目标函数𝐽(𝜃)替换为修改后的PPO 目标，来扩展REINFORCE 或者 Actor-Critic。此修改带来了更稳定、且样本效率更高的训练过程。 在本章中，我们首先讨论性能崩溃问题。随后通过单调改进理论来应对该问题。我们将该理论应 用于策略梯度目标，将其改造为一个代理目标。 在介绍完理论基础后，我们讨论PPO 算法。 8.1 数学...

2. 126 Chapter 8. PPO 背后的数学 定义 8.1 — 连续型随机变量的KL 散度. 𝐷KL(𝑝∥𝑞) = ∫𝑝(𝑥) log 𝑝(𝑥) 𝑞(𝑥)d𝑥 (8.3) 上面的式子是当𝑥为连续型随机变量时的KL 散度。当𝑥为离散型随机变量时，数学式如下所示。 定义 8.2 — 离散型随机变量的KL 散度. 𝐷KL(𝑝∥𝑞) = ∑ 𝑥 𝑝(𝑥) log 𝑝(𝑥) 𝑞(𝑥) (8.4) KL 散度具有以下特性： • 两个概率分布的差异越大，KL 散度的值就越大 • KL 散度的值大于或等于0，且仅当两个概率分布相同时，其值才为0 • KL 散度是非对称的衡量指标，因此𝐷KL(𝑝∥𝑞)和𝐷KL(𝑞∥𝑝)的值不同 这些特性使得KL 散度可以用来衡量两个概率分布的差异程度。下面我们通过具体的例子来了解这 些特性。这里以抛硬币为例进行说明。假设一枚硬币正面朝上和反面朝上的概率是确定的，如下表 所示。 正面朝上的概率 70% 反面朝上的概率 30% 表 8.1 硬币的真实概率分布 上表是硬币的“真实概率分布”，在这里我们用符号𝑝来表示。假设有人对这枚硬币的概率分布做了 如下估计。 正面...

3. 8.1 数学背景知识 127 𝐷KL(𝑝∥𝑞) = 0.7 log 0.7 0.2 + 0.3 log 0.3 0.8 = 0.58⋯ (8.6) 这个值比第一个KL 散度的值要大。最后，假设又有一个人对这枚硬币的概率分布做了如下估计。 正面朝上的概率 70% 反面朝上的概率 30% 表 8.4 第三个人对硬币的概率分布做出的估计 这个分布与真实的概率分布相同。此时的KL 散度的值如下所示。 𝐷KL = 0.7 log 0.7 0.7 + 0.3 log 0.3 0.3 = 0.7 log 1 + 0.3 log 1 (log 1 = 0) = 0 (8.7) 由于𝑝和𝑞是相同的概率分布，因此KL 散度为0。 从以上结果可以看出，KL 散度可以用来衡量两个概率分布的差异程度。当两个概率分布相同时， KL 散度取最小值0。两个概率分布的差异越大，KL 散度的值就越大。 8.1.2 信息论 信息量（Self-Information）： • 一个事件发生的信息量定义为：𝐼(𝑥) = −log 𝑃(𝑥) • 概率越小的事件，包含的信息量越大 • 例如：“太阳从东边升起”（高概率）vs “...

4. 128 Chapter 8. PPO 背后的数学 这就是交叉熵损失！ KL 散度和交叉熵 KL 散度衡量两个分布的差异： 𝐷KL(𝑃∥𝑄) = ∑ 𝑥 𝑃(𝑥) log 𝑃(𝑥) 𝑄(𝑥) = ∑ 𝑥 𝑃(𝑥) log 𝑃(𝑥) −∑ 𝑥 𝑃(𝑥) log 𝑄(𝑥) (8.11) 𝐷KL(𝑃∥𝑄) = −𝐻(𝑃) + 𝐻(𝑃, 𝑄) (8.12) 𝜕𝐷KL(𝑃∥𝑄) 𝜕𝜃 = 𝜕𝐻(𝑃, 𝑄) 𝜕𝜃 (8.13) 其中： • 𝐻(𝑃) = −∑𝑥𝑃(𝑥) log 𝑃(𝑥) 是真实分布的熵（常数） • 𝐻(𝑃, 𝑄) = −∑𝑥𝑃(𝑥) log 𝑄(𝑥) 是交叉熵 最小化KL 散度 ⟺ 最小化交叉熵（因为真实分布的熵是常数） 8.1.3 重要性采样（Importance Sampling） 8.1.3.1 什么是重要性采样？ 重要性采样利用从其他概率分布中采样的数据来计算某个概率分布的期望值。为了说明重要性采 样，下面来看一个计算期望值𝔼𝜋[𝑥]的简单例子。设𝑥是随机变量，𝑥的概率用𝜋(𝑥)表示。期望值的式 子如下所示。 𝔼𝜋[𝑥] = ∑𝑥𝜋(𝑥) (8.14) 我们先复习一下学...

5. 8.1 数学背景知识 129 𝔼𝜋[𝑥] = ∑𝑥𝜋(𝑥) 𝑏(𝑥) 𝑏(𝑥) = 𝔼𝑏[𝑥𝜋(𝑥) 𝑏(𝑥) ] (8.17) 这里需要注意的是式子中的𝔼𝑏，它表示的是概率分布𝑏的期望值。另外每个𝑥都要乘以𝜋(𝑥) 𝑏(𝑥) 这一点也 很重要。如果𝜌(𝑥) = 𝜋(𝑥) 𝑏(𝑥) ，那么我们就可以把式子看作每个𝑥都乘以权重𝜌(𝑥)。根据以上探讨，基于 式的蒙特卡洛方法如下所示。 采样: 𝑥(𝑖) ∼𝑏 (𝑖= 0, 1, ⋯, 𝑛) 𝔼𝜋[𝑥] ≃𝜌(𝑥(1))𝑥(1) + 𝜌(𝑥(2))𝑥(2) + ⋯+ 𝜌(𝑥(𝑛))𝑥(𝑛) 𝑛 (8.18) 这样我们就使用从不同于𝜋的概率分布𝑏中采样的数据计算出了𝔼𝜋[𝑥]。下面来实现重要性采样的代 码 。这里我们对下图所示的概率分布进行重要性采样。 0.8 0.1 1 2 3 1/3 1 2 3 图 8.1 概率分布𝜋和𝑏 这里的目标是求出期望值𝔼𝜋[𝑥]。我们先尝试用普通的蒙特卡洛方法求概率分布𝜋的期望值。代码如 下所示。 用普通的蒙特卡洛方法求概率分布𝜋的期望值 Python 1 import numpy as np 2 3 x ...

6. 130 Chapter 8. PPO 背后的数学 16 17 mean = np.mean(samples) 18 var = np.var(samples) 19 print("MC: {:.2f} (var: {:.2f})".format(mean, var)) # 输出：MC: 2.78 (var: 0.27) 首先，套用定义式求期望值。求出的结果是2.7（这是真实的值）。然后，使用蒙特卡洛方法来求这 个值。这里只基于概率分布pi 采样100 个数据，然后求平均值。为此，我们用NumPy 的np.mean 方法求出了平均值。结果是2.78，接近于真实值。作为参考，我们还使用NumPy 的np.var 方法求 出了方差。方差的值是0.27。在与下一次重要性采样的结果进行比较时，这个值将被用作参考。方 差用于表示数据的离散程度。期望值和方差之间的关系可以用以下式子表示。 Var[𝑋] = 𝔼[(𝑋−𝔼[𝑋])2] (8.19) 方差是数据𝑋和𝑋的平均值𝔼[𝑋]之间的差的平方的期望值。直观地说，它代表了数据的离散程度，如 下图所示。 方差大 方差小 图 8.2 将每个数据作为二...

7. 8.1 数学背景知识 131 这里使用概率分布b 进行采样，不过进行采样的目标是b 的索引([0, 1, 2]) 。这是因为在计算权 重rho 时，要用到的是采样得到的索引。 接下来看看上面的结果。平均值是2.95，虽然与真实值2.7有一些差距，但也还算接近。此外，方 差是10.63，这说明数据的离散程度大于蒙特卡洛方法的结果（使用蒙特卡洛方法时的方差是0.27）。 8.1.3.2 如何减小方差 方差越小，用较少的样本得到的近似值就越准确。反之，方差越大，越需要更多的样本才能得到准 确的近似值。下面介绍如何用重要性采样来减小方差。首先，我们结合下图来了解一下为什么重要 性采样方法的方差会比较大。 0.8 0.1 1 2 3 1/3 1 2 3 图 8.3 基于概率分布𝑏采样3的例子 上图展示了选择3作为样本数据的例子。此时的权重𝜌为2.4。因此，3这个值要乘以2.4。这意味着尽 管我们得到的值是3，但实际得到的值是3 × 2.4 = 7.2。听起来可能有些奇怪，但这是有道理的。原 因如下。 • 由于3是概率分布𝜋的代表值，因此本来它应该被更频繁地采样。 • 但在概率分布𝑏中，3这...

8. 132 Chapter 8. PPO 背后的数学 4 5 for _ in range(n): 6 idx = np.arange(len(b)) # [0, 1, 2] 7 i = np.random.choice(idx, p=b) # 使用 b 进行采样 8 s = x[i] 9 rho = pi[i] / b[i] # 𝜌 10 samples.append(rho * s) 11 12 mean = np.mean(samples) 13 var = np.var(samples) 14 print("IS: {:.2f} (var: {:.2f})".format(mean, var)) # 输出：IS: 2.72 (var: 2.48) 上面的代码假设𝑏的概率分布为[0.2, 0.2, 0.6] ，其形状更接近于pi 的概率分布。结果是，平均 值为2.72，更接近于正确答案。另外，方差是2.48，比以前小。 因此，在进行重要性采样时，可以通过使两个概率分布更加接近来减小方差。不过，强化学习的 重点是让一个策略（概率分布）进行“探索”，另一个策略进行“利用”。在满足这...

## 4. 与前后章关系

- 在线阅读：https://modelshit-class.vercel.app/01-rl/08-ppo-math/
- 本地对照：`python tools/extract_pdf.py "E:\大模型\强化学习\main2.pdf" --start 125 --end 140`
