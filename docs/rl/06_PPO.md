# PPO

> 原书 PDF 第 111–118 页
> 人话版：[Modelshit Class · PPO：稳一点的策略更新](https://modelshit-class.vercel.app/01-rl/06-ppo/)

## 1. 一句话

clip 限制策略更新幅度，防止一次异常奖励把策略拽崩。

## 2. 章节结构

- 6.1 策略梯度法存在的问题
- 6.2 近端策略优化
- 1 = 𝜃𝑘

## 3. 核心要点

1. 6. 近端策略优化（PPO） 提示 PPO: Proximal Policy Optimization，近端策略优化 6.1 策略梯度法存在的问题 回顾一下策略梯度法的公式。 ∇𝜃𝐽(𝜃) = ∇𝜃𝔼𝜏∼𝜋𝜃[𝐺(𝜏)] = 𝔼𝜏∼𝜋𝜃[∑ 𝑇 𝑡=0 𝐺(𝜏)∇𝜃log 𝜋𝜃(𝐴𝑡|𝑆𝑡)] (6.1) 还有梯度上升算法 𝜃←𝜃+ 𝛼∇𝜃𝐽(𝜃) (6.2) 步子迈得太大会导致灾难。但如果步子太小，模型学习速度就会太慢。把奖励函数想象成高耸的山 峰。如果新策略走得太远，它采取的行动可能会差之毫厘，最终从悬崖上掉下来。当我们重新开始 探索时，我们从一个表现不佳的状态开始，采用局部糟糕的策略。性能会崩溃，而且需要很长时间 才能恢复。

2. 112 Chapter 6. 近端策略优化（PPO） 图 6.1 梯度上升算法中步幅太大，策略直接崩溃了 图 6.2 左图是步幅太大，中间的图是理想的步幅，右边的图是方向不对 为什么在深度学习中这个问题没有那么严重呢？ 深度学习中，使用的训练数据集是固定不变的。换句话说，不管神经网络的参数怎么变，训练 数据集是不会变的。也就是说，神经网络的参数和训练数据集没关系。 而强化学习中，训练数据是每一轮根据策略来采样的新的轨迹。而采样的轨迹的质量好坏无 法控制。也就是说训练数据集和策略神经网络的参数是有关系的。这就麻烦了。

3. 6.1 策略梯度法存在的问题 113 在强化学习中很难找到合适的学习率。假设学习率是专门针对上图黄点调整的。该区域相对平坦，因 此为了获得良好的学习速度，学习率应该高于平均值。但是，一步走错，我们就会从悬崖上掉到红 点。红点处的梯度很高，当前的学习率会触发爆炸式策略更新。由于学习率对地形不敏感，策略梯 度算法的收敛问题非常严重。 图 6.3 梯度爆炸更新 策略梯度法存在的问题是训练出来的新策略可能比旧策略还要差 能否找到一个算法，使得训练出来的新策略一定比旧策略好呢？也就是能否保证下面的式子成 立呢？ 𝐽(𝜃new) −𝐽(𝜃old) ≥0 (6.3) 6.2 近端策略优化 在最优化理论中，优化方法主要有两种：线搜索和置信域。梯度下降算法是一种线搜索。我们首先 确定下降方向，然后朝该方向迈出一步。 提示 Trust Region Method: 置信域方法

4. 114 Chapter 6. 近端策略优化（PPO） 图 6.4 左图为线搜索，右图为置信域 而在置信域中，我们确定想要探索的最大步长，然后在该置信域内找到最优点。 图 6.5 置信域方法 也就是，我们在梯度上升的时候，先周围看一圈，然后找一个安全的步长，然后进行梯度上升。 为了实现这个目的，我们的梯度公式∇𝜃𝐽(𝜃)就要改变了。 PPO 算法是Actor-Critic 架构的，也就是演员-评论家架构。 所以我们先来回顾一下策略梯度法的演员-评论家架构的梯度公式。 策略梯度法的目标函数如下

5. 6.2 近端策略优化 115 𝐽(𝜃) = 𝔼𝜏∼𝜋𝜃[𝐺(𝜏)] (6.4) 策略梯度法的目标函数的梯度如下 ∇𝜃𝐽(𝜃) = 𝔼𝜏∼𝜋𝜃[∑ 𝑇 𝑡=0 𝐴𝜋𝜃 𝑡∇𝜃log 𝜋𝜃(𝑎𝑡|𝑠𝑡)] (6.5) 其中𝐴𝜋𝜃 𝑡为估计的优势。这里的𝐴表示优势，也就是Advantage。表示当前策略采取动作𝑎𝑡之后，相 对于价值函数评估的价值的优势是多少。 危险 为了不和前面的公式混淆，我们将动作的数学符号由𝐴𝑡改为𝑎𝑡，将状态的数学符号由𝑆𝑡改为𝑠𝑡。 这样𝐴就代表优势了。 而PPO 的目标函数变成了下式，也就是替代目标函数（surrogate objection function）。 定理 6.1 — 带裁剪的PPO 目标函数. 𝐽(𝜃)ppo-clip = 𝔼𝜏∼𝜋𝜃old[∑ 𝑇 𝑡=0 [min( 𝜋𝜃(𝑎𝑡|𝑠𝑡) 𝜋𝜃old(𝑎𝑡|𝑠𝑡) 𝐴 𝜋𝜃old 𝑡 , clip( 𝜋𝜃(𝑎𝑡|𝑠𝑡) 𝜋𝜃old(𝑎𝑡|𝑠𝑡), 1 −𝜀, 1 + 𝜀) 𝐴 𝜋𝜃old 𝑡 )]] 针对同一个状态𝑠𝑡， 新策略采取动作𝑎𝑡的概率 和旧策略采取动作𝑎𝑡的概率的比值 旧策略 采取...

6. 116 Chapter 6. 近端策略优化（PPO） 危险 在训练最开始时，新策略和旧策略相同，所以比值为𝜋𝜃(𝑎𝑡|𝑠𝑡) 𝜋𝜃old(𝑎𝑡|𝑠𝑡) = 1，也就是这个比值不会被裁 剪。 所以在训练最开始时，也就是PPO 的目标函数第一次进行反向传播时，有如下成立： 𝐽(𝜃)ppo-clip = 𝔼𝜏∼𝜋𝜃old[∑ 𝑇 𝑡=0 𝜋𝜃(𝑎𝑡|𝑠𝑡)|𝜃=𝜃old 𝜋𝜃old(𝑎𝑡|𝑠𝑡) 𝐴 𝜋𝜃old 𝑡 ] (6.8) 对两边进行求导，注意这里求导时𝜃= 𝜃old。所以有如下： ∇𝜃𝐽(𝜃)ppo-clip = ∇𝜃𝔼𝜏∼𝜋𝜃old[∑ 𝑇 𝑡=0 𝜋𝜃(𝑎𝑡|𝑠𝑡)|𝜃=𝜃old 𝜋𝜃old(𝑎𝑡|𝑠𝑡) 𝐴 𝜋𝜃old 𝑡 ] = 𝔼𝜏∼𝜋𝜃old[∑ 𝑇 𝑡=0 ∇𝜃𝜋𝜃(𝑎𝑡|𝑠𝑡)|𝜃=𝜃old 𝜋𝜃old(𝑎𝑡|𝑠𝑡) 𝐴 𝜋𝜃old 𝑡 ]（log 梯度技巧） = 𝔼𝜏∼𝜋𝜃old[∑ 𝑇 𝑡=0 𝐴 𝜋𝜃old 𝑡 ∇𝜃log 𝜋𝜃(𝑎𝑡|𝑠𝑡)] (6.9) 我们会发现，PPO 的第一次反向传播，等价于原始的策略梯度法🏊️。 如果新策略与旧策略的概率比超出...

7. 6.2 近端策略优化 117 𝑝𝑡(𝜃) > 0 𝐴𝑡 min 函数的结果 目标函数是否被裁 剪？ 目标函数的符号 梯度 1 𝜌𝑡(𝜃) ∈[1 −𝜀, 1 + 𝜀] + 𝜌𝑡(𝜃)𝐴𝑡 否 + ✓ 2 𝜌𝑡(𝜃) ∈[1 −𝜀, 1 + 𝜀] - 𝜌𝑡(𝜃)𝐴𝑡 否 - ✓ 3 𝜌𝑡(𝜃) < 1 −𝜀 + 𝜌𝑡(𝜃)𝐴𝑡 否 + ✓ 4 𝜌𝑡(𝜃) < 1 −𝜀 - (1 −𝜀)𝐴𝑡 是 - 0 5 𝜌𝑡(𝜃) > 1 + 𝜀 + (1 + 𝜀)𝐴𝑡 是 + 0 6 𝜌𝑡(𝜃) > 1 + 𝜀 - 𝜌𝑡(𝜃)𝐴𝑡 否 - ✓ 表 6.1 对PPO 目标函数根据比值的不同进行分情况讨论 我们有6 种不同的情况，见上面的表。首先记住，我们取裁剪目标和非裁剪目标中的最小值。 • 情况1 和2：比值介于裁剪范围之内 在情况1 和2 中，由于比例介于范围[1 −𝜀, 1 + 𝜀]之间，因此目标函数不会被裁剪。 在情况1 中，我们具有正优势：该动作优于该状态下所有动作的平均值。因此，我们应该鼓励当 前策略提高在该状态下采取该动作的概率。 由于该比值是在区间之间的，因此我们可以增加我们的策...

8. 118 Chapter 6. 近端策略优化（PPO） 提示 𝐴𝑡不是𝜃的函数。所以对𝜃求导为0。 总而言之，得益于这个裁剪的替代目标函数（替换了原来的简单优美的目标函数（𝐽(𝜃) = 𝔼𝜏∼𝜋𝜃[𝐺(𝜏)]），我们限制了当前策略与旧策略之间的差异范围。因为我们消除了比值超出区间的诱 因，因为裁剪会对梯度产生影响。如果比值为> 1 + 𝜀或< 1 −𝜀，则梯度将等于0。 PPO 增加了一个软约束（裁剪机制），可以通过一阶优化器（求一阶导数）进行优化。我们偶尔 可能会做出一些错误的决策，但它在优化速度上取得了良好的平衡。实验结果证明，这种平衡能够 以最简单的方式实现最佳性能。 带裁剪的PPO 算法 1 输入：初始化策略参数𝜃0 2 初始化裁剪阈值参数𝜀= 0.2 3 for 𝑘= 0, 1, 2, ⋯ do 4 使用策略𝜋𝜃𝑘采集一条轨迹数据𝒟︀𝑘 5 计算优势𝐴 𝜋𝜃𝑘 𝑡 ▷ 使用广义优势估计（GAE） 6 更新策略 ▷ 执行N 步的梯度下降 7 𝜃′ 1 = 𝜃𝑘 ▷ 将𝜃𝑘作为梯度更新的起点 8 for 𝑖= 1, 2, ⋯, 𝑁 do ▷ 更新N 次梯度 9 𝜌= 𝜋𝜃′𝑖...

## 4. 与前后章关系

- 在线阅读：https://modelshit-class.vercel.app/01-rl/06-ppo/
- 本地对照：`python tools/extract_pdf.py "E:\大模型\强化学习\main2.pdf" --start 111 --end 118`
